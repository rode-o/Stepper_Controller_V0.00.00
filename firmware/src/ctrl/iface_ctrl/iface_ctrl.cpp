/* iface_ctrl.cpp
 * --------------
 *  Intentionally empty: the pure-virtual interface has no code to compile.
 *  (File kept only so build systems that expect a .cpp with every folder
 *   don’t complain.)
 */
